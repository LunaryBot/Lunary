const { Command, ContextCommand } = require("../../../structures/Command")
const Discord = require("discord.js")

module.exports = class CleanCommand extends Command {
    constructor(client) {
        super({
            name: "",
            description: "",
            category: "",
            dirname: __dirname,
            permissions: {
                Discord: [""],
                Bot: [""],
                Lunar: [""]
            }
        }, client)
    }

    /** 
    * @param {Discord.CommandInteraction} interaction
    * @param {ObjRef} t
    * @param {ObjRef} db
    */

    async run(ctx, t) {
        
    }
}